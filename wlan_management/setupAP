#1/bin/bash

sudo rm /etc/dhcpcd.conf
sudo rm /etc/network/interfaces
sudo cp ~/pi_server/wlan_management/dhcpcd.conf.AP /etc/dhcpcd.conf && sudo cp ~/pi_server/wlan_management/interfaces.AP /etc/network/interfaces && sudo reboot -h now
